interface:
  display_name: "AI Agent Swarm Codex-only"
  short_description: "Codex-only agent workflow with Custom Agents, engineering gates, and local RAG memory."
  default_prompt: "Use $multi-model-agents / AI Agent Swarm Codex-only. Start from docs/START_PROMPT.md, keep Codex as the orchestrator, use Codex Custom Agents for non-trivial work, and use local RAG only for verified project memory."
  icon_small: "./assets/ai-agent-swarm-icon.png"
  icon_large: "./assets/ai-agent-swarm-icon.png"
  brand_color: "#38BDF8"

dependencies:
  tools:
    - type: "mcp"
      value: "multi-model-agents"
      description: "Local Codex-only MCP tools for config status and local RAG memory."
      transport: "stdio"

policy:
  allow_implicit_invocation: true
